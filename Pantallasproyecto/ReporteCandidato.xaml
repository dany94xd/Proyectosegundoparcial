<Window x:Name="winreportecand" x:Class="ReporteCandidato"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="ReporteCandidato" Height="316.783" Width="300">
    <Grid Background="#FFE5E5E5">
        <DataGrid x:Name="datagribconsulta" HorizontalAlignment="Left" Margin="2,10,0,0" VerticalAlignment="Top" Height="169" Width="280" ItemsSource="{Binding Path=Candidato}">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnmenuprincipal" Content="Menu Principal" HorizontalAlignment="Left" Margin="105,212,0,0" VerticalAlignment="Top" Width="94"/>
    </Grid>
</Window>
